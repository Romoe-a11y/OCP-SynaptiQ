# ── Core data & ML ──
pandas>=2.1,<3.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0
shap>=0.43,<1.0

# ── API (Flask for backend compat, FastAPI for standalone) ──
flask>=3.0,<4.0
gunicorn>=21.2,<23.0
fastapi>=0.104,<1.0
uvicorn[standard]>=0.24,<1.0
pydantic>=2.5,<3.0

# ── Database ──
psycopg2-binary>=2.9,<3.0
sqlalchemy>=2.0,<3.0

# ── HTTP client ──
httpx>=0.25,<1.0

# ── Deep learning (optional — for MOMENT anomaly detection) ──
# Install locally with: pip install torch transformers huggingface-hub momentfm
# These are NOT included in Railway deployment to keep image size manageable.
# The AI service gracefully falls back to IsolationForest when MOMENT is unavailable.
